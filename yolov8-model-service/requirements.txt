fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.11.0
pydantic-settings>=2.8.0
pillow>=11.0.0
numpy>=2.1.0
ultralytics>=8.3.0
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchvision==0.21.0+cu124
requests>=2.31.0
# CLIP (transformers) — проверка «фасад здания vs прочее» перед детекцией дефектов.
# На части сред (Python 3.14 / Windows) установка может потребовать свежие wheels;
# сервис работает и без неё, с ограниченным fallback по пустым детекциям.
transformers>=4.45.0
# NOTE: For maximum defect quality, use a dedicated building-damage YOLO (.pt).
# Поддержка RAW форматов дронов
rawpy>=0.19.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9
# Для чтения метрик модели
pyyaml>=6.0.1

